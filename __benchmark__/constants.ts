import path from 'node:path';

export const BENCHMARK_DIRECTORY = path.join(__dirname, 'benchmark-temp');
export const BENCHMARK_RESULTS_DIRECTORY = path.join(__dirname, 'results');
export const ONE_MB = 1024 * 1024;
